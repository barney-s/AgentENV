PLANNED

## Instance: k8s1

Deployment plan for `agentenv-k8s1` based on runbook `docs-exploration/runbooks/deploy-gke.md`.

### Target Environment & Resolved Parameters
- **GCP Project**: `barni-cnrm-20260529`
- **Region**: `us-central1`
- **Zone**: `us-central1-a`
- **Resource Prefix**: `agentenv-k8s1`
- **Kubernetes Namespace**: `agentenv-system`

### Infrastructure Resources to Create
- **GKE Cluster**: `agentenv-k8s1-cluster`
  - 3 x `n2-standard-4` worker nodes
  - OS Image: `UBUNTU_CONTAINERD`
  - Nested Virtualization: Enabled (`--enable-nested-virtualization`)
  - Labels: `repo-agent-instance=agentenv-k8s1`
- **Node Initializer**: `agentenv-node-initializer` DaemonSet in `kube-system` to load host kernel module `ublk_drv` and configure sysctl limits on GKE worker nodes.
- **Artifact Registry Repository**: `agentenv-k8s1-repo` (`us-central1`, Docker format, labeled `repo-agent-instance=agentenv-k8s1`)
- **Container Images**:
  - `us-central1-docker.pkg.dev/barni-cnrm-20260529/agentenv-k8s1-repo/agentenv-runtime:latest`
  - `us-central1-docker.pkg.dev/barni-cnrm-20260529/agentenv-k8s1-repo/agentenv-gateway:latest`
  - `us-central1-docker.pkg.dev/barni-cnrm-20260529/agentenv-k8s1-repo/agentenv-scheduler:latest`
- **Storage**: GCS bucket `gs://agentenv-k8s1-snapshots` for snapshot repository storage.
- **Kubernetes Workloads** (in `agentenv-system`):
  - `agentenv-gateway` (Deployment & Service)
  - `agentenv-scheduler` (Deployment & Service)
  - `agentenv-node` (Privileged DaemonSet)
  - `agentenv-auth` (Secret with autogenerated API key)
  - ConfigMaps for configuration files

### Estimated Cost
- **GKE Control Plane**: ~$0.10/hour (~$73/month)
- **GCE Worker Nodes** (3 x `n2-standard-4`): ~$0.58/hour (~$420/month)
- **Artifact Registry & GCS Storage**: <$2/month
- **Total Estimated Cost**: ~$0.68/hour (~$16.30/day) while active.

### Feasibility State
- [✓] `gcloud` — present
- [✓] `kubectl` — present (v1.35.8 with Kustomize v5.7.1)
- [✓] `make` — present
- [✓] `git` — present
- [✓] `curl` — present
- [✗] `docker` — **MISSING** (Host environment needs `docker` installed to build and push images)
- [✗] **GKE Cluster Connectivity** — **MISSING** (`agentenv-k8s1-cluster` not yet provisioned; will be created by `deploy.sh`)

### Needs from Owner
- Install Docker (`sudo apt-get update && sudo apt-get install -y docker.io`) on the executing runner to enable `make k8s-build` and `docker push`.
- Ensure active GCP identity (`cnrm-barni-1.svc.id.goog`) has necessary IAM permissions (`roles/container.admin`, `roles/compute.admin`, `roles/artifactregistry.admin`, `roles/iam.serviceAccountUser`).

### Deviations from the Runbook
- All cloud resource names are prefixed with `agentenv-k8s1` and tagged with `repo-agent-instance=agentenv-k8s1` to ensure multi-instance isolation and unambiguous resource ownership.
- Existence guards added prior to cluster and artifact registry creation to ensure idempotency when running `deploy.sh`.

### Notes for Deploy Execution
- Review `docs-exploration/runbook-deployments/k8s1/params.env` for literal resolved values before initiating execution.
- `deploy.sh` automates steps 1 through 8 from `deploy-gke.md`, ending with `make k8s-apply`.
- `teardown.sh` removes the Kubernetes resources, node initializer daemonset, Artifact Registry repository, GKE cluster, and GCS bucket.
